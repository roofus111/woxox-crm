DATABASE_URL="postgresql://woxox:woxox_dev@localhost:5432/woxox_crm?schema=public"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-in-production-use-secrets-manager"
JWT_EXPIRES_IN="10h"
PORT=4001
CORS_ORIGIN="http://localhost:3000"
NODE_ENV=development
APP_ORIGIN="http://localhost:3000"

# Razorpay (optional locally)
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
# RAZORPAY_WEBHOOK_SECRET=

# SMTP for welcome / invite emails (optional — skips send if unset)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="WOXOX <noreply@woxox.com>"
